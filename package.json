{
	"private": true,
	"type": "module",
	"scripts": {
		"client": "nore develop client --debug",
		"server": "nore develop server --debug",
		"client:build": "nore build client --debug",
		"server:build": "nore build server --debug",
		"build": "rm -rf docs && nore build client --debug && mv .nore/client docs && cp -R public/* docs",
		"deploy": "git subtree push --prefix docs origin gh-pages"
	},
	"workspaces": [
		"nore/packages/*"
	],
	"dependencies": {
		"react-device-detect": "1.11.14",
		"react-spring": "^8.0.27"
	}
}
