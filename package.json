{
	"private": true,
	"type": "module",
	"scripts": {
		"client": "nore develop client --debug",
		"server": "nore develop server --debug",
		"client:build": "nore build client --debug",
		"server:build": "nore build server --debug",
		"build": "rm -rf docs && nore build client --debug && nore build server --debug && mv .nore/client docs && mv .nore/server docs && cp -R public/* docs",
		"deploy": "git subtree push --prefix docs origin gh-pages"
	},
	"workspaces": [
		"nore/packages/*"
	],
	"dependencies": {
		"react-device-detect": "1.9.10",
		"react-spring": "^8.0.27"
	}
}
